## Summary

Briefly explain what this PR changes and whether it affects scoped access, global access, or both.

## Changes

- Added ...
- Updated ...
- Fixed ...

## Testing

Describe how this was tested.

- [ ] `composer test` passes
- [ ] `composer analyse` passes
- [ ] `composer format` passes (or `vendor/bin/pint --dirty`)

## Documentation

- [ ] Docs updated in `docs/` and `pnpm docs:build` succeeds (if applicable)

## Checklist

- [ ] Change is narrow and follows existing package patterns
- [ ] No compatibility layers, fallbacks, or legacy paths added
- [ ] Permission/role changes synced via `access:sync` where applicable
- [ ] Generated scaffold code uses `App\...` namespaces (not package namespaces)
- [ ] Ready for review